# Example environment file for Docker builds
# Copy this to .env and adjust values as needed

# Backend API URL (e.g., http://localhost:5000 for development, https://api.example.com for production)
VITE_API_URL=http://backend:5000
